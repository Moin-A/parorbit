import React, { useContext } from "react";
import Context from "./Context";

const Gallery = () => {
  const { page, data } = useContext(Context);
  return <h1>kxanxjn</h1>;
};

export default Gallery;
